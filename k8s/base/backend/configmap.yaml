apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: bookstore-backend
  labels:
    app: bookstore
    component: backend
data:
  NODE_ENV: production
  LOG_LEVEL: info
  PORT: "3000"
